# Telegram-бот на Java 25 с акторной моделью поверх MVStore

## Roadmap

### Этап 0. Скелет проекта

- [x] Maven-проект, Java 25, структура пакетов (`engine`, `telegram`, `game`).
- [x] `pom.xml` с зависимостями: MVStore, JSON-парсер (jackson), Logback.
- [x] Лицензия MIT
- [x] `.gitignore`
- [x] README
- [x] Пустой `main()`, который стартует и останавливается.

### Этап 1. MVStore + бинарные ключи

- [x] Открытие/закрытие MVStore (в проде будет в `MvStoreManager`)
- [x] Кастомный `DataType` для ключа mailbox-а (24 байта: fireDate + UUID v7).
- [x] ~~Генератор UUID v7 (15-20 строк кода)~~ - нет, воспользуемся библиотекой
- [x] Простой тест: положить сообщения с разными fireDate, прочитать в порядке сортировки, убедиться, что отложенные идут после текущих.

### Этап 2. Акторный цикл (один актор)

- [x] Класс `Actor`: mailbox (MVMap), virtual thread, цикл park/parkUntil/process.
- [x] Метод `send(actorId, message)`: запись в MVStore + unpark.
- [ ] TODO Сериализация/десериализация сообщений в JSON (records → JSON → records). **отложено до разных типов сообщений**
- [x] Тест: один актор, кидаем ему сообщения, он их обрабатывает в правильном порядке.
- [x] Тест на отложенные сообщения: fireDate в будущем, актор просыпается вовремя.

### Этап 3. Транзакции и дедупликация

- [x] `TransactionStore`: обработка + удаление + отправка = один commit.
- [x] Карта `processed`, проверка перед обработкой.
- [ ] TODO Тест: симуляция crash (rollback вместо commit) → сообщение обрабатывается повторно → дедупликация срабатывает. **отложено до реализации DLQ**

### Этап 4. Poison message + DLQ

- [ ] Retry-счётчик (in-memory), порог — 3 попытки.
- [ ] Перемещение в DLQ (отдельный MVMap).
- [ ] Актор `dlq-monitor` с self-scheduling.
- [ ] Тест: обработчик кидает исключение → 3 retry → сообщение в DLQ.

